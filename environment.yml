name: app-books
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - requests
  - pillow
  - pyzbar
  - pip:
      - customtkinter
      - opencv-python
prefix: /opt/homebrew/anaconda3/envs/app-books